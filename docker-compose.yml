services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: subscripciones
      POSTGRES_PASSWORD: dev_password
      POSTGRES_DB: subscripciones_db
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: